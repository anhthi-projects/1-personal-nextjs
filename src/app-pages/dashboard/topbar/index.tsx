import { Typography } from "@anhthi-projects/usy-ui";

import { TopBarContainer } from "./topbar.styled";

export const TopBar = () => {
  return (
    <TopBarContainer>
      <Typography>Hello! Thi</Typography>
    </TopBarContainer>
  );
};
